# گزارش کار — فرم ثبت‌نام خریدار و هنرمند/طراح: **جدا**، هر کدام در صفحه‌ی خودش

**شاخه:** `arena/01a0cf92-rozvelt` · **PR:** [#1](https://github.com/milpardi42-max/Rozvelt/pull/1)

## خواسته‌ی شما

> «فرم ثبت‌نام خریدار و هنرمند/طراح را می‌خواهم از هم جدا باشد. چرا گیج شدی؟ الان برو دقیق درست کن.»

حق با شماست: در قدم قبل فرم‌ها را در **یک فرم مشترک** ادغام کرده بودم؛ آن کار برگشت داده شد. الان دو فرم
کاملاً جدا هستند و هرکدام صفحه‌ی خودش را دارد، و انتخاب نوع حساب هم دست خود کاربر است:

| مسیر | چه چیزی آن‌جاست |
|---|---|
| `/{locale}/signup` | **صفحه‌ی انتخاب:** کارت‌های رادیویی «خریدار» و «هنرمند / طراح» + دکمه‌ی «ادامه»؛ خودِ این صفحه هیچ فرم ثبت‌نامی ندارد |
| `/{locale}/signup/buyer` | **فرم جدا و اختصاصی خریدار:** فقط همان چهار فیلد (نام، ایمیل، رمز، تکرار رمز) |
| `/{locale}/signup/artist` | **فرم جدا و اختصاصی هنرمند/طراح:** نام، ایمیل، تلفن، نوع فعالیت، شهر، اینستاگرام، لینک نمونه‌کار، رمز و تکرارش + بلوک اختیاری استودیو |

## چه چیزی ساخته شد

### ۱) صفحه‌ی انتخاب `/{locale}/signup`

- دو کارت انتخاب به‌صورت **رادیوی واقعی** (`name="account_role"`، مقادیر `buyer` و `artist`) با توضیح هر حساب و
  دکمه‌ی «ادامه — ثبت‌نام …». انتخاب با کیبورد هم کار می‌کند و کارت انتخاب‌شده تیک و حاشیه‌ی پررنگ می‌گیرد.
- هر کارت یک **لینک مستقیم** به صفحه‌ی همان نوع هم دارد («رفتن به فرم ثبت‌نام خریدار» / «… هنرمند / طراح»)، پس
  انتخاب حتی بدون جاوااسکریپت کار می‌کند.
- روی خودِ این صفحه **هیچ فرم ثبت‌نامی نیست** (نه `confirm`، نه `studioName`) — فقط انتخاب است.

### ۲) فرم خریدار `/{locale}/signup/buyer`

همان پوسته‌ی کارت ورود با تصویر و پنل «قبلاً ثبت‌نام کردید؟»، و همان فرمی که خریدار همیشه داشته: **نام، ایمیل،
رمز، تکرار رمز** — بدون هیچ فیلد فروشندگی. بالای فرم **سوئیچ نوع حساب** هست و زیر فرم کارت راهنما که طراحان را
به صفحه‌ی خودشان می‌فرستد. ریگ بررسی می‌کند که در این فرم هیچ `phone`/`studioName`/`type`/`account_role` نباشد.

### ۳) فرم هنرمند/طراح `/{locale}/signup/artist`

صفحه‌ی کاملی که همیشه برای فروشندگان بود (فرمت‌های تحویل، رنگ‌بندی، لایسنس، سهم و تسویه، الزامات، طراحان
همراه، پرسش‌های پرتکرار) و انتهایش **فرم جدا و اختصاصی فروشنده**: نام، ایمیل، تلفن، نوع فعالیت، شهر، اینستاگرام،
لینک نمونه‌کار، رمز و تکرارش، و زیر آن بلوک **اختیاری استودیو** (نام استودیو، سابقه، بیو، هفت فرمت تحویل، هشت
دسته‌ی کاری، تیک پذیرش شرایط). فقط چهار فیلد حساب `required` هستند و هیچ گام‌بندی‌ای وجود ندارد؛ زیر هدر صفحه هم
نوار «ثبت‌نام به عنوان: [خریدار | هنرمند / طراح]» هست.

### ۴) رفت‌وآمد بین دو صفحه و مسیرهای قدیمی

- **سوئیچ نوع حساب** (`AccountTypeSwitch`) روی هر دو صفحه‌ی فرم هست؛ صفحه‌ی جاری با `aria-current="page"` علامت
  می‌خورد و یک کلیک به صفحه‌ی دیگر می‌برد.
- `/{locale}/creators/join` (لینک نوار ناوبری، فوتر، صفحه‌ی هنرمندان، راهنمای خریدار و نتایج قدیمی) با **۳۰۷**
  به `/{locale}/signup/artist` می‌رود؛ لینک‌های داخل سایت مستقیم به مسیرهای تازه به‌روز شده‌اند و `sitemap.xml`
  هر سه صفحه را دارد.
- **پروفایل هنرمند کامل جدا از خریدار مانده است:** `/artist` داشبورد هنرمند، `/artist/marketplace` استودیوی
  فروش، `/artist/portfolio` مدیر پورتفولیو؛ خریدار `/account` و در `/artist` صفحه‌ی راهنما.

### ۵) یک اصلاح ظاهری کوچک

کارت ثبت‌نام، ارتفاع همه‌ی `input`ها را ۴۲ پیکسل می‌کند؛ این قاعده حالا رادیوها و چک‌باکس‌ها را مستثنا می‌کند
(`input:not([type="radio"]):not([type="checkbox"])`) تا خودِ رادیوی انتخاب نوع حساب (که عمداً پنهان است) و تیک
شرایط اندازه‌ی درست خودشان را داشته باشند. ریگ همین را از CSS سرو‌شده چک می‌کند.

## تأیید زنده (روی بیلد پروداکشن)

ریگ `scripts/marketplace-smoke/artist-dashboard-e2e.sh` روی سرور تازه اجرا شد: **۹۱ بررسی، همه سبز، بدون هیچ
skip و بدون هیچ هشدار** — از جمله:

- صفحات انتخاب و فرم‌ها: هر دو نوع حساب روی `/{locale}/signup` به‌صورت رادیوی واقعی، هر انتخاب لینک صفحه‌ی خودش،
  نبودن فرم ثبت‌نام روی صفحه‌ی انتخاب، چهار فیلد بودن فرم خریدار و **جدا بودنش** (بدون فیلد فروشنده)، فرم
  فروشنده با همه‌ی فیلدهای خودش و بدون رادیوی نوع حساب، بلوک اختیاری استودیو، اجباری بودن فقط چهار فیلد حساب،
  نبودن هیچ گام‌بندی، و باقی‌ماندن همه‌ی محتوای فروش.
- ریدایرکت: `/{locale}/creators/join` در هر دو زبان با **۳۰۷** به `/{locale}/signup/artist`.
- ثبت‌نام واقعی: پرونده‌ی کامل روی رکورد هنرمند ذخیره می‌شود، حساب بدون هیچ فیلد اضافه هم ساخته می‌شود و برای
  پاسخ‌های خالی چیزی از خودش نمی‌سازد، ایمیل تکراری `email_taken` می‌گیرد، مدیر پرونده را می‌بیند و تأیید
  می‌کند، خریدار به داشبورد هنرمند نمی‌رسد، پورتفولیو سر جایش است و دکمه‌ی خروج در همه‌ی سطوح هست.

بقیه‌ی ریگ‌ها روی همین بیلد سبز شدند: `http` · `exclusive` · `artist` · `sub` · `family` (۳۳) · `formats` (۶۲) ·
`multipart` (۳) · `pages.sh` · ثبت‌نام آکادمی. `npx tsc --noEmit` پاک، `npm run build` موفق. صفحه‌های
`/fa/signup` · `/en/signup` · `/fa/signup/buyer` · `/fa/signup/artist` · `/en/signup/artist` همه ۲۰۰ می‌دهند و
داده‌ی فروشگاه بعد از ریگ تمیز است (۳ کاربر و ۵ هنرمند اصلی).

## چطور در پیش‌نمایش امتحان کنید

1. `/fa/signup` — کارت خریدار یا هنرمند/طراح را انتخاب کنید و «ادامه» بزنید (یا روی لینک همان کارت کلیک کنید).
2. `/fa/signup/buyer` — فرم خریدار با چهار فیلد؛ بالایش سوئیچ نوع حساب.
3. `/fa/signup/artist` — صفحه‌ی کامل فروشندگان با فرم جدا و بلوک اختیاری استودیو؛ زیر هدر نوار انتخاب نوع حساب.
4. `/fa/creators/join` — همان صفحه‌ی ۳ (ریدایرکت ۳۰۷)، تا لینک‌های قدیمی نشکنند.
