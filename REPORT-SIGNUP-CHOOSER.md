# گزارش کار — انتخاب نوع حساب در ثبت‌نام + یک صفحه‌ی جداگانه برای هر نوع

**کامیت:** `78fa051` روی شاخه‌ی `arena/01a0cf92-rozvelt` · **PR:** [#1](https://github.com/milpardi42-max/Rozvelt/pull/1)

## خواسته‌ی شما

> «می‌خواهم در فرم ثبت‌نام؛ انتخاب خریدار - هنرمند/طراح بشه انتخاب کرد. می‌خواهم برای هر قسمت یک صفحه‌ی
> جداگانه باز کنی. لطفاً با دقت بساز و فرم ثبت‌نام را قابل انتخاب کن.»

## چیزی که ساخته شد

ثبت‌نام حالا **سه صفحه** دارد: یک صفحه‌ی انتخاب و یک صفحه‌ی مستقل برای هر نوع حساب.

### ۱) `/{locale}/signup` — صفحه‌ی انتخاب

- دو کارت انتخاب به‌صورت **فرم واقعی** (`input type="radio"` + دکمه‌ی «ادامه») — یعنی همان «قابل انتخاب»
  بودن که خواستید؛ با کیبورد هم کار می‌کند و کارت انتخاب‌شده با تیک و حاشیه‌ی پررنگ مشخص می‌شود.
- کارت **خریدار**: «برای خرید لایسنس طرح‌ها، دانلود فایل‌ها، ذخیره‌ی علاقه‌مندی‌ها و نگه‌داشتن گواهی‌های
  خرید» + برچسب‌های فرم کوتاه · چهار فیلد · دسترسی فوری.
- کارت **هنرمند / طراح**: «برای فروش طرح‌ها با لایسنس: پروفایل هنرمند، فرمت‌های تحویل، سهم فروش و تسویه» +
  برچسب‌های داشبورد هنرمند · بازبینی مدیر · **۴۰٪ سهم فروش** (عدد واقعی از تنظیمات سرور خوانده می‌شود، نه
  دستی در متن).
- روی هر کارت یک **لینک مستقیم** به صفحه‌ی همان نوع هست («رفتن به فرم ثبت‌نام خریدار» / «… هنرمند / طراح»)،
  پس انتخاب با یا بدون جاوااسکریپت کار می‌کند. دکمه‌ی «ادامه» هم صفحه‌ی انتخاب‌شده را باز می‌کند.
- خود صفحه‌ی انتخاب هیچ فرم ثبت‌نامی ندارد؛ فقط انتخاب است.

### ۲) `/{locale}/signup/buyer` — صفحه‌ی خریدار

همان پوسته (کارت ورود با تصویر و پنل «قبلاً ثبت‌نام کردید؟») و همان **چهار فیلد قبلی**: نام، ایمیل، رمز و
تکرار رمز. بالای فرم، **سوئیچ نوع حساب** اضافه شده و زیر فرم همان کارت راهنمای فروشندگان (با لینک تازه)
باقی است.

### ۳) `/{locale}/signup/artist` — صفحه‌ی هنرمند / طراح

کل محتوای صفحه‌ی قبلی `/creators/join` بی‌کم‌وکاست به این مسیر منتقل شد: مزیت‌ها، فرمت‌های تحویل، هشت
دسته‌ی کاری، لایسنس‌ها، «از ثبت‌نام تا اولین فروش»، الزامات، آمار سهم/تسویه، طراحان همراه، **فرم تک‌صفحه‌ای
قبلی فروشنده** با بلوک اختیاری استودیو و پرسش‌های پرتکرار. زیر هدر صفحه یک نوار اضافه شده:
«ثبت‌نام به عنوان: [خریدار | هنرمند / طراح]».

### ۴) سوئیچ نوع حساب در همه‌جا

کامپوننت مشترک `AccountTypeSwitch` روی هر دو صفحه‌ی نوع حساب هست؛ صفحه‌ی جاری با `aria-current="page"`
علامت خورده و یک کلیک به صفحه‌ی دیگر می‌برد. پس هر کسی از هر جایی (سرچ، ایمیل، بوکمارک) وارد شود، نوع
حسابش یک کلیک فاصله دارد.

### ۵) مسیر قدیمی هم می‌شکند نشود

`/{locale}/creators/join` که در نوار ناوبری، فوتر، صفحه‌ی هنرمندان، راهنمای خریدار و نقشه‌ی سایت لینک شده
بود، حالا با **۳۰۷** به `/{locale}/signup/artist` می‌رود (ریدایرکت در `next.config.ts`، سطح روتر). لینک‌های
داخل سایت هم مستقیم به مسیرهای تازه به‌روز شدند و `sitemap.xml` صفحه‌ی انتخاب و هر دو صفحه‌ی نوع حساب را
دارد.

### ۶) چیزهایی که دست نخوردند

- **پروفایل هنرمند کاملاً جدا از خریدار:** `/artist` داشبورد، `/artist/marketplace` استودیوی فروش،
  `/artist/portfolio` پورتفولیو؛ خریدار همچنان `/account` و در `/artist` صفحه‌ی راهنما.
- فرم تک‌صفحه‌ای فروشنده با بلوک اختیاری استودیو، و فرم کوتاه خریدار — هر دو دست‌نخورده.
- API ثبت‌نام، پنل مدیر و کل ساختار بقیه‌ی سایت تغییر نکرد.

## تأیید زنده (روی بیلد پروداکشن)

ریگ `scripts/marketplace-smoke/artist-dashboard-e2e.sh` بازنویسی شد و روی سرور تازه اجرا شد:
**۸۵ بررسی، همه سبز، بدون هیچ skip.** مهم‌ترین‌ها:

- بخش ۱: `/signup` هر دو نوع حساب را به‌صورت انتخاب واقعی (رادیو) نشان می‌دهد، هر انتخاب صفحه‌ی خودش را
  دارد، روی خودِ صفحه‌ی انتخاب هیچ فرم ثبت‌نامی نیست، صفحه‌ی خریدار همان چهار فیلد را دارد و سوئیچ با
  `aria-current` به صفحه‌ی هنرمند لینک می‌شود — و `/{locale}/creators/join` با ۳۰۷ به
  `/{locale}/signup/artist` می‌رود (fa و en).
- بخش ۲: صفحه‌ی هنرمند همان فرم تک‌صفحه‌ای قبلی را سرو می‌کند؛ فقط چهار فیلد حساب `required`،
  استودیو/فرمت‌ها/دسته‌ها/شرایط اختیاری، هیچ گام و دکمه‌ی گامی در HTML نیست، و محتوای فروش سر جایش است.
- بخش ۳ تا ۸: ثبت‌نام واقعی (هم با پرونده‌ی کامل، هم فقط با فیلدهای حساب) روی رکورد هنرمند درست ذخیره
  می‌شود، ایمیل تکراری رد می‌شود، مدیر پرونده را می‌بیند و تأیید می‌کند، خریدار به داشبورد هنرمند نمی‌رسد،
  اعداد داشبورد با API برابر است، پورتفولیو سر جایش است و دکمه‌ی خروج در همه‌ی سطوح هست.

بقیه‌ی ریگ‌ها روی همین بیلد دوباره سبز شدند: `http` · `exclusive` · `artist` · `sub` · `family` (۳۳) ·
`formats` (۶۲) · `multipart` · `pages.sh` · ثبت‌نام آکادمی. `npx tsc --noEmit` پاک و `eslint` صفر خطا.
همه‌ی صفحه‌های تازه ۲۰۰ می‌دهند: `/fa/signup` · `/en/signup` · `/fa/signup/buyer` · `/fa/signup/artist` و
`sitemap.xml` هر دو مسیر تازه را فهرست می‌کند.

## چطور در پیش‌نمایش امتحان کنید

1. `/fa/signup` — کارت‌های خریدار و هنرمند/طراح؛ یکی را انتخاب کنید و «ادامه» را بزنید (یا روی لینک
   «رفتن به فرم …» همان کارت کلیک کنید).
2. `/fa/signup/buyer` — فرم خریدار؛ بالای فرم سوئیچ نوع حساب را ببینید.
3. `/fa/signup/artist` — صفحه‌ی کامل فروشنده با فرم تک‌صفحه‌ای و بلوک اختیاری استودیو؛ زیر هدر نوار
   انتخاب نوع حساب هست.
4. `/fa/creators/join` — همان صفحه‌ی ۳ با ریدایرکت ۳۰۷ (مسیرهای قدیمی نمی‌شکنند).
