# گزارش کار — یک فرم ثبت‌نام برای خریدار و هنرمند/طراح (انتخاب با خود کاربر)

**شاخه:** `arena/01a0cf92-rozvelt` · **PR:** [#1](https://github.com/milpardi42-max/Rozvelt/pull/1)

## خواسته‌ی شما

> «من می‌گم فرم ثبت‌نام خریدار - هنرمند/طراح یکی باشه. چرا گیج شدی؟ من می‌گم کاربر خودش انتخاب کنه.»

پس فرم ثبت‌نام **یکی** شد و **خودِ کاربر** نوع حساب را انتخاب می‌کند — انتخاب نه در یک صفحه‌ی جدا و نه با
پرش به صفحه‌ی دیگر، بلکه **داخل همان فرم**. در همان حال، دو چیزی که خواسته بودید دست‌نخورده مانده‌اند:
**محتوای صفحه‌ی خریدار با محتوای صفحه‌ی هنرمند/طراح تفکیک‌شده** است و **پروفایل/داشبورد هنرمند کاملاً جدا از
خریدار** می‌ماند.

## ۱) یک فرم، با انتخاب نوع حساب داخل آن

کامپوننت تازه `src/components/profile/SignupForm.tsx` **تنها فرم ثبت‌نام سایت** است و هرجا ثبت‌نامی هست همان
یک کامپوننت رندر می‌شود:

- **بالای فرم، انتخاب نوع حساب:** دو کارت رادیویی «خریدار» و «هنرمند / طراح» با
  `name="account_role"` — یک انتخاب واقعی داخل `<form>`، با کیبورد هم کار می‌کند و کارت انتخاب‌شده تیک و
  حاشیه‌ی پررنگ می‌گیرد.
- **کارت خریدار:** «برای خرید لایسنس طرح‌ها، دانلود فایل‌ها، ذخیره‌ی علاقه‌مندی‌ها و نگه‌داشتن گواهی‌های
  خرید» + برچسب‌های «فرم کوتاه · چهار فیلد».
- **کارت هنرمند / طراح:** «برای فروش طرح‌ها با لایسنس: پروفایل هنرمند، فرمت‌های تحویل، سهم فروش و تسویه» +
  «داشبورد هنرمند · بازبینی مدیر · **۴۰٪ سهم فروش**» (عدد از تنظیمات سرور خوانده می‌شود، نه دستی در متن).
- **فرم با انتخاب شما عوض می‌شود:** چهار فیلد حساب (نام، ایمیل، رمز، تکرار رمز) همیشه هستند؛ با انتخاب
  «هنرمند / طراح» نیمه‌ی فروشنده هم باز می‌شود: تلفن، حوزه‌ی فعالیت، شهر، اینستاگرام، لینک نمونه‌کار و زیر آن
  بلوک **اختیاری** پرونده‌ی استودیو (نام استودیو، سابقه، بیو، هفت فرمت تحویل، هشت دسته‌ی کاری، تیک شرایط).
- **دکمه‌ی ثبت هم با نوع حساب عوض می‌شود:** «ثبت‌نام به عنوان خریدار» / «ثبت‌نام به عنوان هنرمند / طراح».

هیچ فیلدی اجباری‌تر از قبل نشده: فقط چهار فیلد حساب در HTML `required` هستند، بقیه اختیاری و بعداً از داشبورد
هنرمند قابل تکمیل. پیام موفقیت هم با نوع حساب عوض می‌شود (خریدار: «حساب خریدار شما ساخته شد»؛ هنرمند: پرونده
برای بازبینی مدیر و انتقال به داشبورد هنرمند).

## ۲) همان یک فرم، روی سه مسیر — بدون صفحه‌ی انتخاب جداگانه

صفحه‌ی «انتخاب نوع حساب» حذف شد؛ هر سه مسیر همان **یک فرم** را سرو می‌کنند:

| مسیر | چه چیزی نشان می‌دهد |
|---|---|
| `/{locale}/signup` | خودِ فرم، با نیمه‌ی خریدار باز (انتخاب نوع حساب داخل فرم) |
| `/{locale}/signup/buyer` | همان فرم زیر عنوان «ثبت‌نام خریدار» + سوئیچ نوع حساب بالای فرم |
| `/{locale}/signup/artist` | صفحه‌ی محتوای فروشندگان (فرمت‌ها، رنگ‌بندی، لایسنس، سهم، تسویه، الزامات، پرسش‌های پرتکرار) که همان فرم را با نیمه‌ی هنرمند باز نشان می‌دهد |

یعنی **محتوا** تفکیک است (صفحه‌ی خریدار کوتاه، صفحه‌ی هنرمند/طراح کامل) ولی **فرم** یکی است. سوئیچ نوع حساب
(`AccountTypeSwitch`) هم فقط بین دو صفحه‌ی محتوا جابه‌جا می‌کند و مسیر قدیمی `/{locale}/creators/join` مثل
قبل با **۳۰۷** به صفحه‌ی هنرمند می‌رود؛ نوار ناوبری، فوتر، صفحه‌ی هنرمندان، راهنمای خریدار و `sitemap.xml` هم
به همین مسیرها اشاره می‌کنند.

## ۳) چیزهایی که دست نخوردند

- **داشبورد هنرمند کاملاً جدا از خریدار** (`/{locale}/artist` شاخص‌ها/آثار/تحویل/کیف پول،
  `/{locale}/artist/marketplace` استودیو، `/{locale}/artist/portfolio` پورتفولیو؛ خریدار `/{locale}/account`
  و در `/artist` صفحه‌ی راهنما).
- API ثبت‌نام، پنل مدیر، سبد خرید، پرداخت و ساختار بقیه‌ی سایت.
- کامپوننت‌های `CreatorSignupForm.tsx` (فرم فروشنده) و `AccountTypeChooser.tsx` (صفحه‌ی انتخاب قبلی) که دیگر
  لازم نبودند و حذف شدند؛ محتوایشان داخل فرم مشترک آمده.

## تأیید زنده (روی بیلد پروداکشن)

ریگ `scripts/marketplace-smoke/artist-dashboard-e2e.sh` بازنویسی و روی سرور تازه اجرا شد:
**۹۵ بررسی، همه سبز، بدون هیچ skip** — از جمله:

- `/{locale}/signup` خودِ فرم را سرو می‌کند و انتخاب نوع حساب یک رادیوی واقعی داخل آن است (fa و en).
- نیمه‌ی فروشنده در همان فرم هست ولی تا وقتی انتخاب نشده `hidden` است؛ خودِ انتخاب و مارک‌آپی که بازش می‌کند
  در باندل کلاینت همان صفحه سرو می‌شود (۱۶ chunk) — پس دکمه‌ها همان چیزی را باز/بسته می‌کنند که در صفحه هست.
- استایل جمع‌و‌جور کارت ورود، اندازه‌ی خودِ رادیوها و تیک شرایط را خراب نمی‌کند (قاعده‌ی `input[type=radio]` در
  CSS سرو‌شده).
- `/{locale}/signup/buyer` همان فرم را زیر عنوان خریدار سرو می‌کند و سوئیچ با `aria-current` به صفحه‌ی
  هنرمند لینک می‌شود.
- `/{locale}/signup/artist` همان فرم را با نیمه‌ی هنرمند **باز** نشان می‌دهد، با همه‌ی محتوای فروش، فقط چهار
  فیلد حساب اجباری، بدون هیچ گام؛ و مسیر قدیمی در هر دو زبان با **۳۰۷** به آن می‌رود.
- ثبت‌نام واقعی: پرونده‌ی کامل روی رکورد هنرمند ذخیره می‌شود، حساب بدون هیچ فیلد اضافه هم ساخته می‌شود و برای
  پاسخ‌های خالی چیزی از خودش نمی‌سازد، ایمیل تکراری رد می‌شود، مدیر پرونده را می‌بیند و تأیید می‌کند، خریدار به
  داشبورد هنرمند نمی‌رسد، پورتفولیو سر جایش است و دکمه‌ی خروج در همه‌ی سطوح هست.

بقیه‌ی ریگ‌ها روی همین بیلد دوباره سبز شدند: `http` · `exclusive` · `artist` · `sub` · `family` (۳۳) ·
`formats` (۶۲) · `multipart` (۳) · `pages.sh` · ثبت‌نام آکادمی. `npx tsc --noEmit` پاک، `npm run build`
موفق و ESLint بدون خطا. صفحه‌های `/fa/signup` · `/en/signup` · `/fa/signup/buyer` · `/fa/signup/artist` ·
`/en/signup/artist` همه ۲۰۰ می‌دهند و داده‌ی فروشگاه بعد از ریگ تمیز است (۳ کاربر، ۵ هنرمند اصلی).

## چطور در پیش‌نمایش امتحان کنید

1. `/fa/signup` — فرم با نیمه‌ی خریدار باز؛ روی کارت «هنرمند / طراح» بزنید: همان فرم باز می‌شود و فیلدهای
   فروشنده (تلفن، حوزه‌ی فعالیت، شهر، اینستاگرام، نمونه‌کار و بلوک اختیاری استودیو) داخل همان فرم ظاهر می‌شوند.
2. `/fa/signup/artist` — همان فرم، این بار با نیمه‌ی هنرمند باز، زیر محتوای کامل فروشندگان.
3. `/fa/signup/buyer` — همان فرم زیر عنوان خریدار، با سوئیچ نوع حساب بالای آن.
4. `/fa/creators/join` — همان صفحه‌ی ۲ با ریدایرکت ۳۰۷ (لینک‌های قدیمی نمی‌شکنند).
